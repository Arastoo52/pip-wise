{
  "name": "pip-wise",
  "private": true,
  "version": "1.0.0",
  "description": "PipWise - Premier Forex Review & Broker Comparison Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
